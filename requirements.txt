fastapi==0.112.2
uvicorn==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
Pillow==10.4.0
numpy==2.1.1
requests==2.32.3
gradio==4.44.0

# Vision (choose one or both; ultralytics pulls torch)
ultralytics==8.3.14
# torch will be installed by ultralytics; for CPU only on laptop it's fine.

# Speech (lightweight)
faster-whisper==1.0.3
av>=15.0.0  # Note: Version 15.1.0 works despite version warning

# TTS (pure python fallback; on Linux may need espeak)
pyttsx3==2.90

# DB
sqlite-utils==3.37

# Additional dependencies for Windows compatibility
python-multipart==0.0.9

# Note: av package is excluded due to installation issues
# Can be added back later when dependencies are resolved
