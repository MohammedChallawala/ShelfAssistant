Demo ideas:
1) Upload tests/shelf.jpg in UI → get detections.
2) Upload tests/query.wav → get transcription.
3) Ask: "Which drink has less sugar?" → see LLM answer composed from data/products.json.

Remember to run: `ollama serve` in another terminal and pull a tiny model like `phi`.
